#include "book.hpp"
#include <cstdlib>

int main() {
  // Basic sanity: create a Book and check its initial state.
  Book book;
  // You can add assertions here, for example:
  // assert(book.is_empty());
  return EXIT_SUCCESS;
}
